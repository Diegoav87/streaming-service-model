if (filteredIndex == 0)
        // {
        //     cout << "No item for this genre was found." << endl;
        //     return;
        // }

        // Video *filtered = new Video[filteredIndex];
        // for (int i = 0; i < size; i++)
        // {
        //     if (videos[i].genre == ngenre)
        //     {
        //         filtered[i] = videos[i];
        //     }
        // }

        // display(filtered, filteredIndex);